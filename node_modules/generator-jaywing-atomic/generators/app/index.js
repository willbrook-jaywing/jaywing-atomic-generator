//-------------------------------------------------------
//  Generator: App
//-------------------------------------------------------

//-------------------------
//  Import
//-------------------------

var _generator = require('yeoman-generator');
var _inquirer = require('inquirer');

//-------------------------
//  Private
//-------------------------

var _promptMainObj = {type: 'list', name: 'menu', message : 'Menu:', choices: ['Components', new _inquirer.Separator(), 'Exit']};

//-------------------------
//  Yeoman Base Extension
//-------------------------

var baseExtension = {

  _promptType: function () {
    return this.prompt(_promptMainObj).then(function (answers) {
      // compose with the user's selected
      switch(answers.menu) {
        case 'Components':
          this.composeWith('jaywing-atomic:components');
          break;
        case 'Component-Modifiers':
          this.composeWith('jaywing-atomic:component-modifiers');
          break;
        case 'Exit':
          break;
      }
    }.bind(this));
  },

  initializing: function () {

    this.log('  ----------------------------------------');
    this.log('          JAYWING ATOMIC GENERATOR        ');
    this.log('  ----------------------------------------');

  },

  prompting: function () {
    this._promptType();
  }

}

module.exports = _generator.Base.extend(baseExtension);