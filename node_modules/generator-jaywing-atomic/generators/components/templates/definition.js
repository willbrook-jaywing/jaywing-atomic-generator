//-------------------------------------------------------
//  Component Module (<%= name %>)
//-------------------------------------------------------

import Module from '../Module';

export default class <%= moduleClassName %> extends Module {

  constructor(el, name, options) {

    // create a list of default settings
    const defaults = {
      "property_one": 'value_one',
      "property_two": 'value_two',
      "property_three": 'value_three'
    };

    // pass variables to the extended class
    super(el, name, options, defaults);

  }

  init() {

    // 'this' reference
    const thisComponent = this;

    // log settings
    console.log('thisComponent.settings.property_one = ' + thisComponent.settings.property_one);
    console.log('thisComponent.settings.property_two = ' + thisComponent.settings.property_two);
    console.log('thisComponent.settings.property_three = ' + thisComponent.settings.property_three);

    // log
    console.log('${this.name} has initialised');

  }

}