//-------------------------------------------------------
//  Component: <%= name %>
//-------------------------------------------------------

import Module from '../Module';

export default class <%= jsClassName %> extends Module {

  constructor(el, name, options) {

    // create a list of default settings
    const defaults = {
      "propertyOne": 'valueOne',
      "propertyTwo": 'valueTwo'
    };

    // pass variables to the extended class
    super(el, name, options, defaults);

  }

  init() {

    // 'this' reference
    const thisComponent = this;

    // log settings
    console.log('thisComponent.settings.propertyOne = ' + thisComponent.settings.propertyOne);
    console.log('thisComponent.settings.propertyTwo = ' + thisComponent.settings.propertyTwo);

    // log
    console.log('${this.name} has initialised');

  }

}