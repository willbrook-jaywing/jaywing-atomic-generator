//-------------------------------------------------------
//  Library: Data Validator
//-------------------------------------------------------

//-------------------------
//  Private
//-------------------------

//-------------------------
//  Public
//-------------------------

var self = module.exports = {

  validateName: function (name) {
    var valid = true;
    var regex = new RegExp(/(^[A-za-z]+( ?[A-za-z0-9]+)*$)/);
    if(!name.match(regex)) valid = 'Use alphanumeric characters seperated by single spaces. No numbers at start.';
    return valid;
  },

  validateSubCategoryName: function (name) {
    // TODO
    var valid = true;
    return valid;
  }

};