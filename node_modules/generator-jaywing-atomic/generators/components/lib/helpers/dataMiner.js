//-------------------------------------------------------
//  Library: Data Syncer
//-------------------------------------------------------

var _fs = require('fs');

var _config = require('../../../components/config');
var _dataHelper = require('../../../components/lib/helpers/dataHelper');

//-------------------------
//  Private
//-------------------------

function _getNameFromSassFolderName(folderName) {
  var strs = folderName.toLowerCase().match(/[^\s-]+/g);
  var result = '';
  for(var i = 0; i < strs.length; i++) {
    result += _.upperFirst(strs[i]) + ' ';
  }
  result = result.trim();
  return result;
}

function _getSassCategoryPath(categoryName) {
  var categoryFolderName = _getSassCategoryFolderName(categoryName);
  var result = _config.componentData.paths.sass + categoryFolderName;
  return result;
}

function _getSassCategoryFolderName(categoryName) {
  var result = name.replace(/\s+/g, '-').toLowerCase();
  return result;
}

//-------------------------
//  Public
//-------------------------

var self = module.exports = {

  getNamesFromSassFolders: function (category) {

    var names = [];

    var path = _getSassCategoryPath(category);
    var exists = _fs.existsSync(path);
    if(exists) {
      var readList = _fs.readdirSync(path);
      for(var i = 0; i < readList.length; i++) {
        var folderName = readList[i];
        var name = _getNameFromSassFolderName(folderName);
        names.push(name);
      }
    }

    return names;

  },

  getSubCategoryNamesFromSassFolders: function (categoryName) {

    var names = [];

    var categoryFolder = _getSassCategoryFolderName(categoryName);
    var path = _config.componentData.paths.modules;
    var exists = _fs.existsSync(path);
    if(exists) {
      var readList = _fs.readdirSync(path);
      for (var i = 0; i < readList.length; i++) {
        var item = readList[i].split('.');
        var name = item[0];
        names.push(name);
      }
    }

    return names;

  },

  getModuleClassNamesFromModulesFolder: function (category) {

    var names = [];

    var path = _config.componentData.paths.modules;
    var exists = _fs.existsSync(path);
    if(exists) {
      var readList = _fs.readdirSync(path);
      for(var i = 0; i < readList.length; i++) {
        var item = readList[i].split('.');
        var name = item[0];
        names.push(name);
      }
    }

    return names;

  },

  getDocsDataAsObj: function () {

    var path = _config.docsData.files.global;
    var json = _fs.readFileSync(path, 'utf8');
    var obj = JSON.parse(json);

    return obj;

  }

};